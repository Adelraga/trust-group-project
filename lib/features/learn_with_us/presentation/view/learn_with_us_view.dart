import 'package:flutter/material.dart';

import '../widgets/learn_with_us_view_body.dart';

class LearnWithUsView extends StatelessWidget {
  const LearnWithUsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: LearnWithUsViewBody(),
    );
  }
}